# Core (DB + config + models) — needed by every command
sqlalchemy>=2.0
psycopg[binary]>=3.1
pydantic>=2.0
pydantic-settings>=2.0

# LLM + geocoding
openai>=1.0
google-genai>=1.0
googlemaps>=4.10.0

# Facebook scraping
playwright>=1.40.0
requests>=2.31.0

# Tests (unit tests only import pure helpers — no browser/DB needed)
pytest>=8.0
